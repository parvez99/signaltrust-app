{
  "name": "llm-gateway",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsup src/server.ts --format esm --platform node --target node20 --out-dir dist --sourcemap --clean --splitting false",
    "start": "node dist/server.js"
  },
  "dependencies": {
    "express": "^4.19.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^20.11.30",
    "tsup": "^8.0.2",
    "typescript": "^5.6.3"
  }
}
